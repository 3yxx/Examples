using UnityEngine;

public class CameraScript : MonoBehaviour
{
    public GameObject player;
    private float offsetY;

    void Start()
    {
        offsetY = 5;
    }

    void Update()
    {
        
        //transform.position = new Vector3(player.transform.position.x + 10, 5,transform.position.z);
        
    }
}
